import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { marginLeft: 10 },
  title: { fontSize: 20, fontWeight: 'bold' },
  imgUrl: { width: 300, height: 200 },
});
